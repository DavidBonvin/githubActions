name: PR Merged Workflow
on:
  pull_request:
    types: [closed]
jobs:
  merge-job:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: 'Message: PR Merged'
        run: echo "PR mergée"